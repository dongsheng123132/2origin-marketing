# STATE · 运营台账

> **任何 AI / 任何新会话接手营销，先读这一页。** 读完就知道现在到哪了、下一步干什么。
> 规则见 [AGENTS.md](AGENTS.md)。这一页只记「当前状态」，不记方法论。

## 引擎现状（2026-08-08）

| 环节 | 跑在哪 | 状态 |
|---|---|---|
| 扫仓库 → 刷新可写素材 | **GitHub Actions**（`.github/workflows/refresh.yml`，每天 08:23 北京时间） | ✅ 已配置，不依赖本机开机 |
| 写草稿 | 本机每日班次 / 会话 | 🔁 cron `052e50d3`，每天 9:37 |
| **一线新闻官**（发现进行时） | 对话当场记线索 → 班次成稿 | ✅ 2026-08-08 建岗，见 `press/README.md` |
| **审核官**（七道闸门 + 定期挤压） | 本机 cron `b9041bb7`，每天 20:43 | ✅ 2026-08-08 建岗，见 `press/审核官.md`；队列 `press/发布队列.md` |
| 发布 | **只有用户能决定** | 🔒 铁律，AI 不碰 |

**两条进料管道，别搞混**：
- `docs/whats-new.md` ← 扫 commit（已落地的东西）→ 写进 `drafts/`
- `press/线索/` ← 对话里的发现（还没进 git）→ 核实后写进 `press/发现/`

扫描器**结构性看不见**对话里的发现——概念在被实现之前没有 commit hash。
新闻官就是补这个洞的：**在东西进 git 之前先抓住它。** 线索会凉，commit 不会，所以班次先做线索。

`refresh` 那一步是纯扫描、不需要 AI、不需要密钥，所以能完全托管在 GitHub 上。
写文章需要判断「哪个点值得写、数字对不对」，这一步没有自动化，**也不该假装它自动化了**。

## 草稿库

| 草稿 | 素材来源 | 状态 |
|---|---|---|
| `2026-08-07-AI组装机时代.md` | 核心叙事 | 📝 待审 |
| `2026-08-07-Codex-CLI中文教程.md` | 长尾 SEO | 📝 待审 |
| `2026-08-08-2Origin本源计算架构.md` | 架构叙事 | 📝 待审 |
| `2026-08-08-uking官网意见反馈.md` | uking 官网反馈区 | 📝 待审 |
| `2026-08-08-conformance一条命令自测.md` | `e523380` | 📝 待审 |
| `2026-08-08-5分钟跑通AI计算机.md` | `6b1da9f` | 📝 待审 |

**6 篇草稿 + `press/发现/` 1 篇 = 7 篇全部待用户审，一篇都没发。**
👉 **完整队列（含卡在 AI 身上的 4 条线索、3 条驳回）看 `press/发布队列.md`，那是真相源。**

## 已核数字（2026-08-09 实跑，发文前请重跑）

- **★ 状态变更（2026-08-09）：本象 v0.1 已装并通过自检 14/14**（`node benxiang/verify-benxiang.mjs`）。
  「七个部件装了六个、缺本象」这个诊断**已过期**——诊断次日部件就装上了，第 5 次复发被 X7.1 修掉。
- Conformance：~~PASS 9~~ → **PASS 10 · FAIL 0 · MANUAL 2**（2026-08-09 重跑，C11 外部对照加入） —— 仍不是「全部通过」，那 2 项是 `[MANUAL]`（2026-08-09 重跑；C8/C9/C10 落地后从 5 涨到 9）
- harness：~~34~~ → ~~47~~ → **62 tests · 62 pass**（2026-08-09 重跑，隔离跑；与 conformance 并发会假失败）
  - 其中 bugscope 专项 **11 条**（A1×3 A2×1 A3×1 A4×4 A5×2）—— **2026-08-09 重跑：A5 已有实现+2 条测试，
    昨日「A5 零测试」已失效**；`lib/bugscope.js` 五条公理全部有实现
  - ⚠️ **2026-08-10 复核：公理已增至六条（新增 A6），但三处 A5 实例仍在**——
    ① `philosophy/bugscope.md:24` 标题已改「六条公理」，**但 :96 仍写「三条公理」**（漏改）；
    ② **A6 零实现零测试**（`grep -c A6 lib/bugscope.js` = 0，测试同为 0）；
    ③ **修复未 push**：`git show origin/master:philosophy/bugscope.md | grep -c 六条公理` = **0**，公网仍是旧文
    → `press/发现/2026-08-09-透视镜照自己.md` 已按此重写，**初稿的「五条」说法已作废**
- 学历 facts：~~204/204/refuted 0~~ → **386 条 · verified=true 371 · verified=false 15 · 带 actor 133**（17 份，2026-08-10 重跑）
  - ★ **「否」首次出现**（0→15），但**仍无 `refuted_by`**：那 15 条字段只有 claim/verified/when，**连 source 都没有**
  - ★ **新查出不对称**：`addFact` 中 `if (verified) { 检查 source }` —— **立「是」要证据，立「否」不要**
  - ★ **律⑤代谢昨日零命中 → 今日已实现**（`lib/state.js:178` autoDeprecate 保鲜期 + refreshFact）
  - 两日内 96 → 114 → **204**，**refuted 恒为 0**。机制见 `2origin-harness/lib/state.js:126`：
    `addFact(state, claim, source, verified = true)` —— **默认参数就是 true**，且 schema 无 `refuted_by`
  - 对照组（同一文件 :137/:161）：`learnings` 有 `status` 状态机 candidate→verified→deprecated
- U-King：0.9.92

## 待办（下次接手从这里开始）

1. **等用户审草稿**——6 篇积压，没审就不能发。发布权在用户，不要自作主张。
2. 还没写的公开素材：见 `docs/whats-new.md` 里标 🚀 的条目（RFC-0005 本境持久状态、session hooks、动作面收敛为 3 核心 等）。
3. 写完一篇 → 往 `docs/covered.json` 加一条 → 那条 commit 下次就标 ✅ 不再重复推荐。

## 历史教训（别再犯）

- **2026-08-08**：AGENTS.md 里的「Conformance 7/7、harness 28 测试」是错的，实测 5+2MANUAL / 34。
  差点把没验证的说成验证过了。**数字一律现跑现写，不许照抄文档。**
- **2026-08-08**：`docs/whats-new.md` 曾把**私有仓库** `u-king-mini` 的 commit 标题自动推到**公开仓库**。
  已改成公私分流（私有的进 `whats-new.local.md`，不提交）。**不要给 Actions 加私有仓库 PAT。**
- **2026-08-08**：曾对外汇报「cron 每天 8:23 自动跑」，实际那是会话级任务，会话一结束就没了，
  从未真正存在过。现在换成 GitHub Actions —— **说自动化之前，先确认它在没有本机的情况下也能跑。**

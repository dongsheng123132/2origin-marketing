# 草稿 · 发布待审

> **状态**：📝 草稿，待用户审。审过后才允许发布。
> **叙事定调**：2026-08-08 用户拍板——「AI 组装机时代」从比喻变成可验证的技术架构（2Origin）。
> **调性**：谦虚诚实。首测 7/7 通过、但诚实标注了跨模型的验证过程，如实呈现。
> **北极星**：文末 CTA 一律指向 u-king.org（下载 U-King = 装机 = 产生 token 消耗）。

- **标题**：我们不造模型，我们造一台 AI 计算机：2Origin 本源计算架构开源了
- **slug**：2origin-ai-computer-architecture
- **summary**：模型是 CPU，但 CPU 从来不等于一台计算机。2Origin 定义了"AI 计算机"该怎么组成——模型可换、状态不丢、动作可迁移、经验会复利，且全部开源可验证。
- **tags**：AI计算机,2Origin,组装机,U-King,开源架构,本境

---

```html
<h1>我们不造模型，我们造一台 AI 计算机</h1>

<p>上个月我们说了「AI 进入组装机时代」——模型是 CPU，Agent 是主板，用户在搭自己的机器。今天，这个比喻终于有了工程化的骨架。</p>

<p>我们把这套架构开源了，叫 <strong>2Origin 本源计算架构</strong>。</p>

<p>一句话：<strong>模型可换，状态不丢，动作可迁移，经验会复利。</strong></p>

<h2>AI 计算机缺的不是 CPU，是主板、硬盘和操作系统</h2>

<p>今天的 Agent 栈已经有一堆好零件：模型（CPU）、上下文窗口（内存）、MCP（总线）、各种 Agent 框架（内核）。但它们拼不成一台<strong>计算机</strong>——因为缺的是把它们装成整机的主板、硬盘、驱动和操作系统。</p>

<p>最普遍的痛点是：<strong>今天教会，明天忘记。这个任务学会，下一个 Session 又从小学一年级开始。</strong>因为 Agent 保存的是聊天记录（影子），不是对象状态（本象）。</p>

<h2>2Origin 的定义：一台会成长的机器</h2>

<p>2Origin 把「AI 计算机」拆成几个明确的部件：</p>
<ul>
  <li><strong>本象</strong>保存世界——AI 不再通过截图/OCR 猜对象，而是直接面对对象与状态</li>
  <li><strong>本境</strong>保存成长——AI 的"学历"，跨会话、跨模型、跨机器继承</li>
  <li><strong>影核</strong>改变世界——统一动作，落到 GUI/CLI/API 任一驱动</li>
  <li><strong>北桥负责知，南桥负责行</strong></li>
</ul>

<h2>不是 PPT，是能跑的东西</h2>

<p>这套架构不是画饼，我们做了可核验的证据：</p>
<ul>
  <li><strong>Conformance 7/7 通过</strong>：跨会话、跨 Harness、跨模型、动作可迁移、结果可验证、学习不自动永久化、可审计，全部实测</li>
  <li><strong>参考实现 28 个测试全过</strong>：零依赖纯 Node，clone 下来 5 分钟跑通完整闭环</li>
  <li><strong>AI 会"转学"</strong>：把学历复制到另一台机器，新机器开学即续</li>
  <li><strong>越用越懂你这台机器</strong>：5 份学历、27 条事实，跨会话一个不丢地带进新会话</li>
</ul>

<p>诚实地说：跨模型的验证我们一开始标错了，后来纠正、用真实第二端点补测——<strong>证据必须诚实，不为好看调状态</strong>。</p>

<h2>U-King 是第一台 2Origin 参考整机</h2>

<p>这套架构的第一台参考整机，就是 U-King——AI 装机管家。装好 Codex / Claude Code / OpenClaw / Hermes，国内直连不用翻墙，免费下载。</p>

<p>今天做这套架构的副产品：<strong>顺带修了 uking 一个真实 bug</strong>——crashlog 反复误报"没正常退出"，用这套架构的"观察→判据→修复"方法论确诊是多实例残留，不是真崩溃。</p>

<h2>开源，且可复刻</h2>

<p>三个仓库，全部 Apache-2.0 开源：</p>
<ul>
  <li>2origin-computer：架构规范 + Conformance 标准</li>
  <li>2origin-harness：零依赖参考实现 + QUICKSTART</li>
  <li>2origin：本象协议</li>
</ul>

<p>任何人都能照着规范，搭出自己的 AI 计算机。</p>

<p style="margin-top:2em;border-left:4px solid #888;padding-left:1em">
  <strong>下载 U-King</strong>，装好你的 AI 全家桶 → https://u-claw.org.cn/download/U-King-Setup.exe<br>
  它是第一台按 2Origin 架构组装的机器。
</p>
```
